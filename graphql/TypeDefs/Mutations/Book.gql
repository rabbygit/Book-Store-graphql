type Book {
    id: ID
    title: String!
    description: String
    author_id: ID
}

input BookInput {
    title: String!
    description: String
    author_id: ID!
}

type Mutation {
  addBook(input: BookInput): Book
}